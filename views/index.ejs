<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog App - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Blog App</a>
            <div class="navbar-nav ms-auto">
                <% if (user) { %>
                    <a class="nav-link" href="/posts">All Posts</a>
                    <a class="nav-link" href="/posts/new">New Post</a>
                    <a class="nav-link" href="/logout">Logout (<%= user.name %>)</a>
                <% } else { %>
                    <a class="nav-link" href="/login">Login</a>
                    <a class="nav-link" href="/register">Register</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8 mx-auto">
                <div class="jumbotron bg-light p-5 rounded">
                    <h1 class="display-4">Welcome to Blog App</h1>
                    <% if (user) { %>
                        <p class="lead">Hello, <strong><%= user.name %></strong>! Ready to share your thoughts?</p>
                        <a class="btn btn-primary btn-lg" href="/posts/new">Write a Post</a>
                        <a class="btn btn-secondary btn-lg" href="/posts">View All Posts</a>
                    <% } else { %>
                        <p class="lead">Join our community and start sharing your ideas!</p>
                        <a class="btn btn-primary btn-lg" href="/register">Get Started</a>
                        <a class="btn btn-outline-secondary btn-lg" href="/login">Login</a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
