# Application Configuration
NODE_ENV=development
PORT=3000
SESSION_SECRET=your-super-secret-session-key

# Database Configuration (for local development without Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blogapp
DB_USER=bloguser
DB_PASSWORD=blogpass123

# Note: When using Docker Compose, these values are set in docker-compose.yml
# The app uses RDS_* environment variables in production mode